import tkinter as tk
from tkinter import ttk
import DictionaryCompare as dic
import UserInterface as user

if __name__ == '__main__':

    user.read_inputs()















    # def read_user_inputs():

    #
    #
    #
    # root = tk.Tk()
    # root.title("Dictionary-Comparison")

    #
    # input1 = ttk.Entry(root)
    # input1.pack()
    # ttk.Label(root, text="Press the button to start").pack()
    #
    # ttk.Button(root, command=read_user_inputs).pack()
    #
    # root.mainloop()

    # label1 = tk.Label(root, text="Dictionary", bg="green")
    # Layout-manager pack
    # label1.pack(side="left", fill="y", expand=True)

    # image = Image.open("images/seeeebbb.jpg").resize((100,50))
    # photo = ImageTk.PhotoImage(image)
    #
    # label_ttk = ttk.Label(root)
    # label_ttk.pack()
    #
    # label_ttk.configure(text="Dictionary", image=photo)

    # button = ttk.Button(root, text="Press", command=say_hello)
    # button.pack()
    #
    # for items in button.keys():
    #     print(items, ":", button[items])
    #

    # inputs_from_user = dic.get_user_input()
    # all_possible_words = dic.get_all_combinations(inputs_from_user)
    # letters_dictionary = dic.get_words_from_dictionary()
    # existing_words = dic.compare_dictionary_to_combinations(letters_dictionary, all_possible_words)
    # dic.display_possible_words(existing_words)
